<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<main>
  <h1>All Recipes</h1>

  <!-- <% recipes.forEach(recipe => { %>
    <div class="card" style="width: 18rem;">
      <img class="card-img-top" src="<%= recipe.imageUrl %>" alt="<%= recipe.name %> Image">
      <div class="card-body">
        <h5 class="card-title"><%= recipe.name %></h5>
        <p class="card-text">by <%= recipe.author.name %></p>
        <a href="/recipes/<%= recipe._id %>" class="btn btn-primary">View Recipe</a>
      </div>
    </div>
  <% }) %> -->
  


  <main>
    <div class="container py-5 px-3">
      <% recipes.forEach(recipe => { %>
        <div class="row p-4">
          <div class="col">
            <div class="card mb-3" style="max-width: 100%;">
              <div class="row g-0">
                <div class="col-md-4">
                  <a class="text-decoration-none" href="/recipes/<%= recipe._id %>">
                    <% if (recipe.imageUrl) { %>
                      <img 
                      class="img-fluid rounded-start"
                      src="<%= recipe.imageUrl %>" 
                      alt="<%= recipe.name %> Image"
                      />
                    <% } else {%>
                      <img
                        class="img-fluid rounded-start"
                        src="https://i.imgur.com/F7q0fQO.jpeg"
                        alt="A placeholder image"
                      />
                    <% } %>
                  </a>
                  </div>
                <div class="col-md-8">
                  <div class="card-body">
                    <h2 class="card-title"><%= recipe.name %></h2>
                    <h3></h3>
                    <% if (recipe.description.length > 300) { %>
                      <% let splitDesc = recipe.description.split(' ') %>
                      <% let editDesc = splitDesc.slice(0, 75).join(' ') %> 
                      <p class="card-text"><%= editDesc %> . . .</p>
                    <% } else { %>
                      <p class="card-text"><%= recipe.description %></p>
                    <% } %>
                    <a class="card-text" href="/recipes/<%= recipe._id %>" class="btn btn-primary">View Recipe</a>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
        <% }) %>



      <!-- <div 
        class="row row-cols-lg-4 g1 px-4"
      >
      <% recipes.forEach(recipe => { %>
        <div class="col">
          <a class="text-decoration-none" href="/recipes/<%= recipe._id %>">
            <div class="card mb-4 shadow-lg">
              <% if (recipe.imageUrl) { %>
                <img 
                class="card-img-top" 
                src="<%= recipe.imageUrl %>" 
                alt="<%= recipe.name %> Image"
                width="100%"
                >
              <% } else {%>
                <img
                  class="card-img-top"
                  src="https://i.imgur.com/F7q0fQO.jpeg"
                  alt="A placeholder image"
                  width="100%"
                />
              <% } %>
              <div class="card-body">
                <p class="card-title fs-3 text-dark"><%= recipe.name %></p>
                <p class="card-title fs-4 text-dark">by <%= recipe.author.name %></p>
                <a href="/recipes/<%= recipe._id %>" class="btn btn-primary">View Recipe</a>
              </div>
            </div>
          </a>
        </div>
      <% }) %>
      </div> -->
    </div>
  </main>


</main>

<%- include('../partials/footer') %>
